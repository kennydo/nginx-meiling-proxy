[tox]
envlist = flake8,py35

[testenv]
deps = 
    -rrequirements.txt
    pytest
commands = pytest

[testenv:flake8]
basepython = python3.5
deps = flake8
commands = flake8

[flake8]
exclude = .git,.tox,build,venv
max-line-length = 120

[tool:pytest]
testpaths = tests
